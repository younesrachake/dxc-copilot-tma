# ============ Core ============
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# ============ Database ============
sqlalchemy==2.0.23
aiosqlite==0.19.0
alembic==1.12.1

# ============ Authentication ============
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# ============ Images / OCR ============
pillow==10.1.0
easyocr==1.7.1
pymupdf==1.23.8

# ============ RAG / Vector DB ============
numpy<2.0
chromadb==0.4.18
rank-bm25==0.2.2

# ============ Local ML (embeddings, reranking, clustering) ============
# torch comes via easyocr — do not pin it here
sentence-transformers==2.7.0
transformers==4.39.3
scikit-learn==1.3.2

# ============ LLM ============
openai==1.3.7
groq==0.11.0

# ============ Validation ============
pydantic==2.5.2
pydantic-settings==2.14.2

# ============ Rate Limiting ============
slowapi==0.1.9

# ============ Document Parsing / Generation ============
python-docx==1.1.0
reportlab==4.2.2

# ============ Observability ============
prometheus-fastapi-instrumentator==6.1.0
python-json-logger==2.0.7

# ============ Utilities ============
python-dotenv==1.0.0
httpx==0.25.2
psutil==5.9.8
types-psutil==5.9.5.20240516
